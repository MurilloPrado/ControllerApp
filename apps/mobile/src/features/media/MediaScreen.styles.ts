import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
  },

  topSection: {
    flex: 1.2,
    justifyContent: "center",
    alignItems: "center",
  },

  bottomSection: {
    flexDirection: "row",
    alignItems: "center",
    marginBottom: 60,
  },

  left: {
    flex: 1,
    alignItems: "center",
  },

  right: {
    flex: 1,
    alignItems: "center",
  }
});